% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mat_fun.R
\name{mat_fun}
\alias{mat_fun}
\title{Analyse length or age at maturity}
\usage{
mat_fun(mat, x, grouping_var = NULL, data, times = 1000)
}
\arguments{
\item{mat}{Binary maturity variable (0 = immature, 1 = mature)}

\item{x}{Continuous predictor variable (e.g. length, age)}

\item{grouping_var}{Optional categorical grouping variable (e.g. sex). Will be converted to a factor.}

\item{data}{A data frame containing the above variables}

\item{times}{Number of bootstrap replicates (default 1000)}
}
\value{
A nested tibble of class \code{"mat_fun"} with one row per group,
containing list columns \code{data}, \code{coefs}, \code{preds}, and \code{mods},
each named by the grouping variable level.
}
\description{
Fits a logistic regression model (binomial GLM) of maturity stage as a
function of length, age, or another continuous predictor. Bootstrap
confidence intervals are computed via \code{rsample}.
}
\examples{
data(spottail)

lm50 <- mat_fun(maturity_stage, length, sex, data = spottail, times = 100)

summary(lm50)

plot(lm50)
}
